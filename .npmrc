# Expo + NativeWind need Metro to resolve nested RN packages from app source.
node-linker=hoisted
# Prevent a second react-native-css-interop under nativewind (breaks className in prod).
public-hoist-pattern[]=*react-native-css-interop*
