import { Stack } from "expo-router";

/** Layout for /t web URL aliases → teacher routes. */
export default function TeacherAliasLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
