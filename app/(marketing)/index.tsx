import LandingPage from "@/components/marketing/LandingPage";

/** Signed-out home. Unique href so nested teacher/student stacks can leave after sign-out. */
export default function MarketingHome() {
  return <LandingPage />;
}
